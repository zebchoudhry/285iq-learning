[project]
name = "285iq-learning"
version = "1.0.0"
requires-python = ">=3.11"
description = "285IQ GCSE learning platform"

[tool.vercel]
entrypoint = "wsgi:application"

# Migrations: run locally or via `vercel env pull` + `python manage.py migrate`
# (build-time migrate often fails if DATABASE_URL is not available to the build).
